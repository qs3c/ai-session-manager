# ai-session-manager 同步闭环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 桌面应用，扫描本机 Codex / Claude Code 会话，通过 GitHub 私有仓库跨设备同步（含路径重映射恢复），并管理会话元数据（rename / description / tags / star）。

**Architecture:** Tauri 2 应用。全部核心逻辑（jsonl 解析、git 同步、路径重映射）在 Rust 后端，以纯函数/模块实现并用 `cargo test` 覆盖；Tauri command 层只做薄封装；React 前端只负责展示与触发。GitHub 仓库是唯一数据真身：`sessions/<sessionId>/{transcript.jsonl, meta.json}` + `devices/<device-id>.json`，元数据冲突按 `updated_at` 整文件 last-write-wins。git 操作通过调用系统 `git` CLI 完成（用户已配置好凭据，不自己管 token）。

**Tech Stack:** Tauri 2, Rust (serde/serde_json, uuid, dirs, anyhow, tempfile), React + TypeScript (Vite), 系统 git CLI。

---

## 背景知识（执行者必读）

执行者假定不了解 Codex / Claude Code 的存储格式。以下是本机实测事实：

**Claude Code 会话**：`~/.claude/projects/<munged-cwd>/<session-uuid>.jsonl`
- 目录名是 cwd 的转码：**每个非字母数字字符替换为 `-`**。例：`E:\fromGithub\ai-session-manager` → `E--fromGithub-ai-session-manager`（`:` 和 `\` 各变一个 `-`）；`C:\Users\27499\.foo` → `C--Users-27499--foo`。大小写保留。
- 每行一个 JSON 对象。消息行（`type` 为 `user`/`assistant`）带 `uuid`、`parentUuid`、`timestamp`、`sessionId`、`cwd`、`gitBranch`、`message: {role, content}`（content 是字符串或块数组）。还有 `queue-operation` 等非消息行（没有 `cwd`）。
- **恢复到新设备时必须**：按新 cwd 重算目录名，并重写每行的 `cwd` 字段。

**Codex 会话**：`~/.codex/sessions/YYYY/MM/DD/rollout-<ISO时间>-<uuid>.jsonl`
- 首行 `{"type":"session_meta","payload":{"id":..., "cwd":..., "timestamp":...}}`。
- 其余行 `type` 为 `response_item`（`payload.type=="message"`，含 `role` 和 `content:[{type:"input_text"|"output_text", text}]`）、`turn_context`（含 `cwd`、`workspace_roots`）、`event_msg` 等。
- 用户真实输入是 `role=="user"` 且文本不以 `<` 开头的 input_text（以 `<` 开头的是注入的 environment_context 等）。
- **恢复时必须**：重写 `session_meta.payload.cwd` 和每个 `turn_context.payload.cwd` / `workspace_roots`。

**同步仓库布局**（数据真身）：

```
sync-repo/
├── devices/<device-id>.json      # {device_id, hostname, os, path_mappings, last_sync_at}
└── sessions/<sessionId>/
    ├── transcript.jsonl          # 原始会话文件的逐字节拷贝（MVP 不压缩，git 自会压缩）
    └── meta.json                 # 见 SessionMeta 结构，updated_at 做 LWW
```

**硬性约束**：
- 绝不修改 `~/.claude` / `~/.codex` 下的原始文件（恢复时只新增文件）。
- 所有读写 jsonl / json 一律 UTF-8（Rust 默认即是；不要经过 PowerShell 管道处理文件内容）。
- 解析必须防御性：未知字段忽略（serde_json::Value 按需取字段，不做全量强类型建模），坏行跳过不 panic。

## File Structure

```
ai-session-manager/
├── docs/superpowers/plans/2026-07-05-sync-core.md   (本文件)
├── src-tauri/
│   ├── Cargo.toml
│   └── src/
│       ├── main.rs          # 模板生成，不动
│       ├── lib.rs           # 模块声明 + Tauri commands（薄封装，Task 11）
│       ├── model.rs         # SessionSource/SessionSummary/SessionMeta/DeviceInfo (Task 2)
│       ├── paths.rs         # munge_claude_project_dir + PathMapping 重映射 (Task 3)
│       ├── scan_claude.rs   # 扫描解析 ~/.claude/projects (Task 4)
│       ├── scan_codex.rs    # 扫描解析 ~/.codex/sessions (Task 5)
│       ├── config.rs        # 本地配置：device_id、repo_url (Task 6)
│       ├── gitcli.rs        # git CLI 封装 (Task 7)
│       ├── syncrepo.rs      # 仓库布局读写 + meta LWW (Task 8)
│       ├── sync.rs          # 上传/拉取编排 (Task 9)
│       └── restore.rs       # 路径重映射恢复 (Task 10)
├── src-tauri/tests/
│   ├── fixtures/            # 两种格式的最小样例 jsonl (Task 4/5 创建)
│   └── e2e_two_devices.rs   # 双设备端到端集成测试 (Task 12)
└── src/                     # React 前端 (Task 13-14)
    ├── api.ts               # invoke 封装
    ├── App.tsx
    └── components/
        ├── SessionList.tsx
        ├── RestoreDialog.tsx
        └── SettingsPanel.tsx
```

依赖关系：Task 2/3 是地基 → 4/5（扫描）、6/7 独立 → 8 依赖 2 → 9 依赖 7/8 → 10 依赖 3/8 → 11 汇总 → 12 集成测试 → 13/14 前端。

---

### Task 1: 项目脚手架

**Files:**
- Create: 整个 Tauri 2 + React 模板（工具生成）
- Modify: `src-tauri/Cargo.toml`

- [ ] **Step 1: 生成 Tauri 2 模板**

当前目录已有 `docs/`，脚手架先生成到子目录再搬上来。**子目录名必须是 `ai-session-manager`**——create-tauri-app 用目录名做包名，Task 12 的集成测试按 lib 名 `ai_session_manager_lib` 引用。PowerShell 执行：

```powershell
npm create tauri-app@latest ai-session-manager -- --template react-ts --manager npm --yes
Get-ChildItem ai-session-manager -Force | Move-Item -Destination .
Remove-Item ai-session-manager -Recurse -Force
npm install
```

搬完后打开 `src-tauri/Cargo.toml` 确认 `[lib] name`（模板通常生成 `ai_session_manager_lib`），如不同，记下实际名字，Task 12 引用时要用它。

- [ ] **Step 2: 补充 Rust 依赖**

编辑 `src-tauri/Cargo.toml`，在 `[dependencies]` 中确保/加入（serde 系模板通常已有）：

```toml
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
dirs = "6"
anyhow = "1"

[dev-dependencies]
tempfile = "3"
```

- [ ] **Step 3: 验证编译**

Run: `cargo check`（在 `src-tauri/` 下）
Expected: 编译通过，无 error。

- [ ] **Step 4: git init 并提交**

```powershell
git init
git add -A
git commit -m "chore: scaffold tauri2 + react app with plan doc"
```

（模板自带 .gitignore，含 node_modules/target。）

---

### Task 2: 核心数据模型 model.rs

**Files:**
- Create: `src-tauri/src/model.rs`
- Modify: `src-tauri/src/lib.rs`（加 `pub mod model;`）

- [ ] **Step 1: 写失败的测试 + 结构体骨架**

创建 `src-tauri/src/model.rs`：

```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "kebab-case")]
pub enum SessionSource {
    Codex,
    ClaudeCode,
}

/// 本机扫描出的一个会话（不落盘，仅内存与 IPC）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SessionSummary {
    pub session_id: String,
    pub source: SessionSource,
    pub cwd: String,
    /// 本机会话文件绝对路径
    pub transcript_path: String,
    /// 恢复时需要的原始相对路径：
    /// codex 是 "2026/06/21/rollout-....jsonl"，claude 是 "<munged-dir>/<id>.jsonl"
    pub relative_store_path: String,
    /// 第一条真实用户消息，截断 200 字符，作为默认标题
    pub first_message: String,
    pub message_count: u32,
    pub started_at: String,
    pub last_active_at: String,
}

/// 同步仓库中 sessions/<id>/meta.json 的内容（数据真身）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SessionMeta {
    pub schema_version: u32,
    pub session_id: String,
    pub source: SessionSource,
    pub title: String,
    #[serde(default)]
    pub description: String,
    #[serde(default)]
    pub tags: Vec<String>,
    #[serde(default)]
    pub starred: bool,
    pub origin_device_id: String,
    pub origin_cwd: String,
    pub relative_store_path: String,
    pub started_at: String,
    pub last_active_at: String,
    /// ISO8601，整文件 last-write-wins 的比较键
    pub updated_at: String,
}

impl SessionMeta {
    /// 从扫描结果初始化一份 meta（首次上传时用）
    pub fn from_summary(s: &SessionSummary, device_id: &str, now_iso: &str) -> SessionMeta {
        SessionMeta {
            schema_version: 1,
            session_id: s.session_id.clone(),
            source: s.source,
            title: s.first_message.clone(),
            description: String::new(),
            tags: vec![],
            starred: false,
            origin_device_id: device_id.to_string(),
            origin_cwd: s.cwd.clone(),
            relative_store_path: s.relative_store_path.clone(),
            started_at: s.started_at.clone(),
            last_active_at: s.last_active_at.clone(),
            updated_at: now_iso.to_string(),
        }
    }
}

/// devices/<device-id>.json
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeviceInfo {
    pub device_id: String,
    pub hostname: String,
    pub os: String,
    #[serde(default)]
    pub path_mappings: Vec<crate::paths::PathMapping>,
    pub last_sync_at: String,
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn meta_json_round_trip() {
        let meta = SessionMeta {
            schema_version: 1,
            session_id: "abc-123".into(),
            source: SessionSource::ClaudeCode,
            title: "修复登录 bug".into(),
            description: String::new(),
            tags: vec!["work".into()],
            starred: true,
            origin_device_id: "dev-1".into(),
            origin_cwd: r"E:\fromGithub\bkcrab".into(),
            relative_store_path: r"E--fromGithub-bkcrab/abc-123.jsonl".into(),
            started_at: "2026-07-05T02:46:20.453Z".into(),
            last_active_at: "2026-07-05T03:00:00.000Z".into(),
            updated_at: "2026-07-05T03:00:01.000Z".into(),
        };
        let json = serde_json::to_string_pretty(&meta).unwrap();
        assert!(json.contains("\"claude-code\"")); // kebab-case 序列化
        let back: SessionMeta = serde_json::from_str(&json).unwrap();
        assert_eq!(back.session_id, "abc-123");
        assert!(back.starred);
    }

    #[test]
    fn meta_missing_optional_fields_defaults() {
        // 老版本 meta.json 缺 tags/starred/description 时必须能解析（向后兼容）
        let json = r#"{
            "schema_version": 1, "session_id": "x", "source": "codex",
            "title": "t", "origin_device_id": "d", "origin_cwd": "/a",
            "relative_store_path": "p.jsonl",
            "started_at": "2026-01-01T00:00:00Z", "last_active_at": "2026-01-01T00:00:00Z",
            "updated_at": "2026-01-01T00:00:00Z"
        }"#;
        let m: SessionMeta = serde_json::from_str(json).unwrap();
        assert_eq!(m.tags.len(), 0);
        assert!(!m.starred);
    }
}
```

在 `src-tauri/src/lib.rs` 顶部加：

```rust
pub mod model;
```

注意：`DeviceInfo` 引用了 `crate::paths::PathMapping`（Task 3 才创建）。为让本 Task 独立编译，**本步先注释掉 `path_mappings` 字段及其 `#[serde(default)]`**，Task 3 完成后再取消注释——或者直接把 Task 3 的 `paths.rs` 中 `PathMapping` 结构体先建出来（推荐后者，见 Step 2）。

- [ ] **Step 2: 创建 PathMapping 占位（属于 paths.rs，但 DeviceInfo 需要）**

创建 `src-tauri/src/paths.rs`（Task 3 会补全函数）：

```rust
use serde::{Deserialize, Serialize};

/// 把其他设备的路径前缀映射到本机路径前缀
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PathMapping {
    pub from_prefix: String,
    pub to_prefix: String,
}
```

`lib.rs` 加 `pub mod paths;`。

- [ ] **Step 3: 运行测试确认失败→通过**

Run: `cargo test model` （在 `src-tauri/` 下）
Expected: 先因编译错误失败（如有笔误），修复后 2 个测试 PASS。

- [ ] **Step 4: Commit**

```powershell
git add src-tauri/src/model.rs src-tauri/src/paths.rs src-tauri/src/lib.rs
git commit -m "feat: core data models with serde round-trip tests"
```

---

### Task 3: 路径转码与重映射 paths.rs

**Files:**
- Modify: `src-tauri/src/paths.rs`

- [ ] **Step 1: 写失败的测试**

在 `src-tauri/src/paths.rs` 追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn munge_windows_path() {
        assert_eq!(
            munge_claude_project_dir(r"E:\fromGithub\ai-session-manager"),
            "E--fromGithub-ai-session-manager"
        );
        // 隐藏目录的点也变 '-'
        assert_eq!(
            munge_claude_project_dir(r"C:\Users\27499\.understand-anything-repo"),
            "C--Users-27499--understand-anything-repo"
        );
    }

    #[test]
    fn munge_posix_path_preserves_case() {
        assert_eq!(munge_claude_project_dir("/Users/sybil/code/App"), "-Users-sybil-code-App");
    }

    #[test]
    fn remap_longest_prefix_wins_and_converts_separators() {
        let maps = vec![
            PathMapping { from_prefix: r"E:\fromGithub".into(), to_prefix: "/Users/sybil/code".into() },
            PathMapping { from_prefix: r"E:\fromGithub\special".into(), to_prefix: "/opt/special".into() },
        ];
        // 更长的前缀优先
        assert_eq!(
            remap_path(r"E:\fromGithub\special\x", &maps).unwrap(),
            "/opt/special/x"
        );
        // 剩余部分的 '\' 转成目标风格的 '/'
        assert_eq!(
            remap_path(r"E:\fromGithub\bkcrab\sub", &maps).unwrap(),
            "/Users/sybil/code/bkcrab/sub"
        );
    }

    #[test]
    fn remap_posix_to_windows() {
        let maps = vec![PathMapping {
            from_prefix: "/Users/sybil/code".into(),
            to_prefix: r"E:\fromGithub".into(),
        }];
        assert_eq!(remap_path("/Users/sybil/code/app", &maps).unwrap(), r"E:\fromGithub\app");
    }

    #[test]
    fn remap_no_match_returns_none() {
        assert!(remap_path("/nowhere", &[]).is_none());
    }
}
```

- [ ] **Step 2: 运行确认编译失败**

Run: `cargo test paths`
Expected: FAIL — `munge_claude_project_dir` / `remap_path` 未定义。

- [ ] **Step 3: 实现**

在 `paths.rs` 的 `PathMapping` 定义后追加：

```rust
/// Claude Code 项目目录名规则：每个非 ASCII 字母数字字符替换为 '-'
pub fn munge_claude_project_dir(cwd: &str) -> String {
    cwd.chars()
        .map(|c| if c.is_ascii_alphanumeric() { c } else { '-' })
        .collect()
}

/// 按最长前缀匹配重映射路径；剩余部分的分隔符转换为目标风格。
/// 无匹配返回 None（调用方让用户手输）。
pub fn remap_path(path: &str, mappings: &[PathMapping]) -> Option<String> {
    let best = mappings
        .iter()
        .filter(|m| path.starts_with(m.from_prefix.as_str()))
        .max_by_key(|m| m.from_prefix.len())?;
    let rest = &path[best.from_prefix.len()..];
    // 目标是 Windows 风格（形如 "X:\..."）用 '\'，否则用 '/'
    let target_sep = if best.to_prefix.get(1..3) == Some(":\\") { '\\' } else { '/' };
    let rest_converted: String = rest
        .chars()
        .map(|c| if c == '\\' || c == '/' { target_sep } else { c })
        .collect();
    Some(format!("{}{}", best.to_prefix, rest_converted))
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test paths`
Expected: 5 个测试 PASS。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/src/paths.rs
git commit -m "feat: claude project dir munging and cross-device path remapping"
```

---

### Task 4: 扫描 Claude Code 会话 scan_claude.rs

**Files:**
- Create: `src-tauri/tests/fixtures/claude/E--fake-proj/11111111-aaaa-bbbb-cccc-000000000001.jsonl`
- Create: `src-tauri/src/scan_claude.rs`
- Modify: `src-tauri/src/lib.rs`（加 `pub mod scan_claude;`）

- [ ] **Step 1: 创建 fixture**

创建 `src-tauri/tests/fixtures/claude/E--fake-proj/11111111-aaaa-bbbb-cccc-000000000001.jsonl`，内容 4 行（模拟真实格式：首行队列操作无 cwd，然后 user/assistant 消息，content 兼有字符串和块数组两种形态），文件必须 UTF-8 无 BOM：

```jsonl
{"type":"queue-operation","operation":"enqueue","timestamp":"2026-07-05T02:46:20.332Z","sessionId":"11111111-aaaa-bbbb-cccc-000000000001","content":"帮我修 bug"}
{"parentUuid":null,"isSidechain":false,"type":"user","message":{"role":"user","content":"帮我修 bug"},"uuid":"u-1","timestamp":"2026-07-05T02:46:20.453Z","cwd":"E:\\fake\\proj","sessionId":"11111111-aaaa-bbbb-cccc-000000000001","version":"2.1.197","gitBranch":"main"}
{"parentUuid":"u-1","isSidechain":false,"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"好的，我看一下。"}]},"uuid":"a-1","timestamp":"2026-07-05T02:46:25.000Z","cwd":"E:\\fake\\proj","sessionId":"11111111-aaaa-bbbb-cccc-000000000001","version":"2.1.197","gitBranch":"main"}
{"parentUuid":"a-1","isSidechain":false,"type":"user","message":{"role":"user","content":"谢谢"},"uuid":"u-2","timestamp":"2026-07-05T02:50:00.000Z","cwd":"E:\\fake\\proj","sessionId":"11111111-aaaa-bbbb-cccc-000000000001","version":"2.1.197","gitBranch":"main"}
```

- [ ] **Step 2: 写失败的测试**

创建 `src-tauri/src/scan_claude.rs`，先只写测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    fn fixtures_root() -> PathBuf {
        PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures/claude")
    }

    #[test]
    fn parse_fixture_session() {
        let path = fixtures_root().join("E--fake-proj/11111111-aaaa-bbbb-cccc-000000000001.jsonl");
        let s = parse_session_file(&path).unwrap();
        assert_eq!(s.session_id, "11111111-aaaa-bbbb-cccc-000000000001");
        assert_eq!(s.cwd, r"E:\fake\proj");
        assert_eq!(s.first_message, "帮我修 bug");
        assert_eq!(s.message_count, 3); // 2 user + 1 assistant，queue-operation 不算
        assert_eq!(s.started_at, "2026-07-05T02:46:20.453Z");
        assert_eq!(s.last_active_at, "2026-07-05T02:50:00.000Z");
        assert_eq!(s.relative_store_path, "E--fake-proj/11111111-aaaa-bbbb-cccc-000000000001.jsonl");
        assert!(matches!(s.source, crate::model::SessionSource::ClaudeCode));
    }

    #[test]
    fn scan_dir_finds_all_sessions() {
        let sessions = scan(&fixtures_root()).unwrap();
        assert_eq!(sessions.len(), 1);
    }

    #[test]
    fn scan_missing_dir_returns_empty() {
        let sessions = scan(&PathBuf::from("Z:/definitely/not/here")).unwrap();
        assert!(sessions.is_empty());
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test scan_claude`
Expected: FAIL — `parse_session_file` / `scan` 未定义。

- [ ] **Step 4: 实现**

在 `scan_claude.rs` 测试模块上方写：

```rust
use crate::model::{SessionSource, SessionSummary};
use anyhow::{Context, Result};
use serde_json::Value;
use std::fs;
use std::io::{BufRead, BufReader};
use std::path::Path;

/// 从 message.content（字符串或块数组）提取纯文本
fn content_text(content: &Value) -> String {
    match content {
        Value::String(s) => s.clone(),
        Value::Array(blocks) => blocks
            .iter()
            .filter_map(|b| {
                if b.get("type").and_then(Value::as_str) == Some("text") {
                    b.get("text").and_then(Value::as_str).map(str::to_string)
                } else {
                    None
                }
            })
            .collect::<Vec<_>>()
            .join("\n"),
        _ => String::new(),
    }
}

fn truncate_chars(s: &str, max: usize) -> String {
    s.chars().take(max).collect()
}

/// 解析单个 Claude Code 会话文件。坏行跳过；无任何消息行返回 Err。
pub fn parse_session_file(path: &Path) -> Result<SessionSummary> {
    let file = fs::File::open(path).with_context(|| format!("open {}", path.display()))?;
    let reader = BufReader::new(file);

    let session_id = path
        .file_stem()
        .and_then(|s| s.to_str())
        .context("bad filename")?
        .to_string();
    let project_dir = path
        .parent()
        .and_then(|p| p.file_name())
        .and_then(|s| s.to_str())
        .context("no parent dir")?
        .to_string();

    let mut cwd = String::new();
    let mut first_message = String::new();
    let mut message_count: u32 = 0;
    let mut started_at = String::new();
    let mut last_active_at = String::new();

    for line in reader.lines() {
        let Ok(line) = line else { continue };
        let Ok(v) = serde_json::from_str::<Value>(&line) else { continue };
        let typ = v.get("type").and_then(Value::as_str).unwrap_or("");
        if typ != "user" && typ != "assistant" {
            continue;
        }
        // 子代理侧链不计入主对话
        if v.get("isSidechain").and_then(Value::as_bool) == Some(true) {
            continue;
        }
        // 有 type 无 message 的行（如 attachment 增量）跳过
        let Some(message) = v.get("message") else { continue };
        message_count += 1;

        if cwd.is_empty() {
            if let Some(c) = v.get("cwd").and_then(Value::as_str) {
                cwd = c.to_string();
            }
        }
        if let Some(ts) = v.get("timestamp").and_then(Value::as_str) {
            if started_at.is_empty() {
                started_at = ts.to_string();
            }
            last_active_at = ts.to_string();
        }
        if first_message.is_empty() && typ == "user" {
            if let Some(content) = message.get("content") {
                let text = content_text(content);
                if !text.is_empty() && !text.starts_with('<') {
                    first_message = truncate_chars(&text, 200);
                }
            }
        }
    }

    anyhow::ensure!(message_count > 0, "no messages in {}", path.display());
    Ok(SessionSummary {
        session_id: session_id.clone(),
        source: SessionSource::ClaudeCode,
        cwd,
        transcript_path: path.to_string_lossy().into_owned(),
        relative_store_path: format!("{}/{}.jsonl", project_dir, session_id),
        first_message,
        message_count,
        started_at,
        last_active_at,
    })
}

/// 扫描 ~/.claude/projects 根目录（传入的是 projects 目录本身）
pub fn scan(projects_root: &Path) -> Result<Vec<SessionSummary>> {
    let mut out = Vec::new();
    let Ok(projects) = fs::read_dir(projects_root) else { return Ok(out) };
    for proj in projects.flatten() {
        if !proj.path().is_dir() {
            continue;
        }
        let Ok(files) = fs::read_dir(proj.path()) else { continue };
        for f in files.flatten() {
            let p = f.path();
            if p.extension().and_then(|e| e.to_str()) == Some("jsonl") {
                if let Ok(s) = parse_session_file(&p) {
                    out.push(s);
                }
            }
        }
    }
    Ok(out)
}
```

`lib.rs` 加 `pub mod scan_claude;`。

- [ ] **Step 5: 运行测试确认通过**

Run: `cargo test scan_claude`
Expected: 3 个测试 PASS。

- [ ] **Step 6: Commit**

```powershell
git add src-tauri/src/scan_claude.rs src-tauri/tests/fixtures src-tauri/src/lib.rs
git commit -m "feat: scan and parse claude code sessions"
```

---

### Task 5: 扫描 Codex 会话 scan_codex.rs

**Files:**
- Create: `src-tauri/tests/fixtures/codex/2026/06/21/rollout-2026-06-21T10-54-52-22222222-aaaa-bbbb-cccc-000000000002.jsonl`
- Create: `src-tauri/src/scan_codex.rs`
- Modify: `src-tauri/src/lib.rs`（加 `pub mod scan_codex;`）

- [ ] **Step 1: 创建 fixture**

创建 `src-tauri/tests/fixtures/codex/2026/06/21/rollout-2026-06-21T10-54-52-22222222-aaaa-bbbb-cccc-000000000002.jsonl`，内容 5 行（首行 session_meta；一条以 `<` 开头的注入 user 消息要被跳过；一条真实 user；一条 assistant；一条 turn_context），UTF-8 无 BOM：

```jsonl
{"timestamp":"2026-06-21T02:54:57.499Z","type":"session_meta","payload":{"id":"22222222-aaaa-bbbb-cccc-000000000002","timestamp":"2026-06-21T02:54:52.130Z","cwd":"E:\\fake\\proj","originator":"codex_vscode","cli_version":"0.142.0"}}
{"timestamp":"2026-06-21T02:54:57.602Z","type":"response_item","payload":{"type":"message","role":"user","content":[{"type":"input_text","text":"<environment_context>\n  <cwd>E:\\fake\\proj</cwd>\n</environment_context>"}]}}
{"timestamp":"2026-06-21T02:54:57.602Z","type":"turn_context","payload":{"turn_id":"t-1","cwd":"E:\\fake\\proj","workspace_roots":["e:\\fake\\proj"],"model":"gpt-5.5"}}
{"timestamp":"2026-06-21T02:54:57.626Z","type":"response_item","payload":{"type":"message","role":"user","content":[{"type":"input_text","text":"帮我重构这个函数"}]}}
{"timestamp":"2026-06-21T02:55:10.000Z","type":"response_item","payload":{"type":"message","role":"assistant","content":[{"type":"output_text","text":"好的，先看现有实现。"}]}}
```

- [ ] **Step 2: 写失败的测试**

创建 `src-tauri/src/scan_codex.rs`，先只写测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    fn fixtures_root() -> PathBuf {
        PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures/codex")
    }

    #[test]
    fn parse_fixture_rollout() {
        let root = fixtures_root();
        let path = root.join("2026/06/21/rollout-2026-06-21T10-54-52-22222222-aaaa-bbbb-cccc-000000000002.jsonl");
        let s = parse_session_file(&path, &root).unwrap();
        assert_eq!(s.session_id, "22222222-aaaa-bbbb-cccc-000000000002");
        assert_eq!(s.cwd, r"E:\fake\proj");
        // 以 '<' 开头的注入消息被跳过，取到真实的第一条
        assert_eq!(s.first_message, "帮我重构这个函数");
        assert_eq!(s.message_count, 3); // 三条 response_item message（含注入行）
        assert_eq!(s.started_at, "2026-06-21T02:54:52.130Z"); // session_meta.payload.timestamp
        assert_eq!(s.last_active_at, "2026-06-21T02:55:10.000Z");
        assert_eq!(
            s.relative_store_path,
            "2026/06/21/rollout-2026-06-21T10-54-52-22222222-aaaa-bbbb-cccc-000000000002.jsonl"
        );
        assert!(matches!(s.source, crate::model::SessionSource::Codex));
    }

    #[test]
    fn scan_walks_date_tree() {
        let sessions = scan(&fixtures_root()).unwrap();
        assert_eq!(sessions.len(), 1);
    }

    #[test]
    fn scan_missing_dir_returns_empty() {
        assert!(scan(&PathBuf::from("Z:/nope")).unwrap().is_empty());
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test scan_codex`
Expected: FAIL — 函数未定义。

- [ ] **Step 4: 实现**

在 `scan_codex.rs` 测试模块上方写：

```rust
use crate::model::{SessionSource, SessionSummary};
use anyhow::{Context, Result};
use serde_json::Value;
use std::fs;
use std::io::{BufRead, BufReader};
use std::path::Path;

fn input_text(payload: &Value) -> String {
    payload
        .get("content")
        .and_then(Value::as_array)
        .map(|blocks| {
            blocks
                .iter()
                .filter_map(|b| b.get("text").and_then(Value::as_str))
                .collect::<Vec<_>>()
                .join("\n")
        })
        .unwrap_or_default()
}

fn truncate_chars(s: &str, max: usize) -> String {
    s.chars().take(max).collect()
}

/// 解析单个 rollout 文件。sessions_root 用于算 relative_store_path。
pub fn parse_session_file(path: &Path, sessions_root: &Path) -> Result<SessionSummary> {
    let file = fs::File::open(path).with_context(|| format!("open {}", path.display()))?;
    let reader = BufReader::new(file);

    let mut session_id = String::new();
    let mut cwd = String::new();
    let mut started_at = String::new();
    let mut first_message = String::new();
    let mut message_count: u32 = 0;
    let mut last_active_at = String::new();

    for line in reader.lines() {
        let Ok(line) = line else { continue };
        let Ok(v) = serde_json::from_str::<Value>(&line) else { continue };
        let typ = v.get("type").and_then(Value::as_str).unwrap_or("");
        let Some(payload) = v.get("payload") else { continue };

        match typ {
            "session_meta" => {
                session_id = payload.get("id").and_then(Value::as_str).unwrap_or("").to_string();
                cwd = payload.get("cwd").and_then(Value::as_str).unwrap_or("").to_string();
                started_at = payload
                    .get("timestamp")
                    .and_then(Value::as_str)
                    .unwrap_or("")
                    .to_string();
            }
            "response_item" => {
                if payload.get("type").and_then(Value::as_str) != Some("message") {
                    continue;
                }
                let role = payload.get("role").and_then(Value::as_str).unwrap_or("");
                if role != "user" && role != "assistant" {
                    continue; // developer 等注入角色不算
                }
                message_count += 1;
                if let Some(ts) = v.get("timestamp").and_then(Value::as_str) {
                    last_active_at = ts.to_string();
                }
                if first_message.is_empty() && role == "user" {
                    let text = input_text(payload);
                    if !text.is_empty() && !text.starts_with('<') {
                        first_message = truncate_chars(&text, 200);
                    }
                }
            }
            _ => {}
        }
    }

    anyhow::ensure!(!session_id.is_empty(), "no session_meta in {}", path.display());
    let relative_store_path = path
        .strip_prefix(sessions_root)
        .with_context(|| "path not under sessions root")?
        .to_string_lossy()
        .replace('\\', "/");

    Ok(SessionSummary {
        session_id,
        source: SessionSource::Codex,
        cwd,
        transcript_path: path.to_string_lossy().into_owned(),
        relative_store_path,
        first_message,
        message_count,
        started_at,
        last_active_at,
    })
}

/// 递归扫描 ~/.codex/sessions（YYYY/MM/DD 三层日期树）
pub fn scan(sessions_root: &Path) -> Result<Vec<SessionSummary>> {
    let mut out = Vec::new();
    walk(sessions_root, sessions_root, &mut out);
    Ok(out)
}

fn walk(dir: &Path, root: &Path, out: &mut Vec<SessionSummary>) {
    let Ok(entries) = fs::read_dir(dir) else { return };
    for e in entries.flatten() {
        let p = e.path();
        if p.is_dir() {
            walk(&p, root, out);
        } else if p.extension().and_then(|x| x.to_str()) == Some("jsonl") {
            if let Ok(s) = parse_session_file(&p, root) {
                out.push(s);
            }
        }
    }
}
```

`lib.rs` 加 `pub mod scan_codex;`。

- [ ] **Step 5: 运行测试确认通过**

Run: `cargo test scan_codex`
Expected: 3 个测试 PASS。

- [ ] **Step 6: Commit**

```powershell
git add src-tauri/src/scan_codex.rs src-tauri/tests/fixtures/codex src-tauri/src/lib.rs
git commit -m "feat: scan and parse codex rollout sessions"
```

---

### Task 6: 本地配置 config.rs

**Files:**
- Create: `src-tauri/src/config.rs`
- Modify: `src-tauri/src/lib.rs`（加 `pub mod config;`）

本地配置只存两样：`device_id`（首次运行生成的 UUID，标识本设备）和 `repo_url`。路径映射表存在仓库的 `devices/<device-id>.json` 里（Task 8），不在这里。

- [ ] **Step 1: 写失败的测试**

创建 `src-tauri/src/config.rs`，先只写测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn first_load_generates_device_id_and_persists() {
        let dir = tempfile::tempdir().unwrap();
        let c1 = AppConfig::load_or_init(dir.path()).unwrap();
        assert_eq!(c1.device_id.len(), 36); // uuid v4
        assert!(c1.repo_url.is_none());

        // 再次加载得到同一个 device_id
        let c2 = AppConfig::load_or_init(dir.path()).unwrap();
        assert_eq!(c1.device_id, c2.device_id);
    }

    #[test]
    fn save_and_reload_repo_url() {
        let dir = tempfile::tempdir().unwrap();
        let mut c = AppConfig::load_or_init(dir.path()).unwrap();
        c.repo_url = Some("git@github.com:me/ai-sessions.git".into());
        c.save(dir.path()).unwrap();
        let back = AppConfig::load_or_init(dir.path()).unwrap();
        assert_eq!(back.repo_url.as_deref(), Some("git@github.com:me/ai-sessions.git"));
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test config`
Expected: FAIL — `AppConfig` 未定义。

- [ ] **Step 3: 实现**

在测试模块上方写：

```rust
use anyhow::Result;
use serde::{Deserialize, Serialize};
use std::fs;
use std::path::Path;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AppConfig {
    pub device_id: String,
    #[serde(default)]
    pub repo_url: Option<String>,
}

impl AppConfig {
    /// config_dir 是应用配置目录（生产环境用 tauri 的 app_config_dir，测试用 tempdir）
    pub fn load_or_init(config_dir: &Path) -> Result<AppConfig> {
        let path = config_dir.join("config.json");
        if path.exists() {
            let text = fs::read_to_string(&path)?;
            return Ok(serde_json::from_str(&text)?);
        }
        let config = AppConfig {
            device_id: uuid::Uuid::new_v4().to_string(),
            repo_url: None,
        };
        config.save(config_dir)?;
        Ok(config)
    }

    pub fn save(&self, config_dir: &Path) -> Result<()> {
        fs::create_dir_all(config_dir)?;
        fs::write(
            config_dir.join("config.json"),
            serde_json::to_string_pretty(self)?,
        )?;
        Ok(())
    }
}
```

`lib.rs` 加 `pub mod config;`。

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test config`
Expected: 2 个测试 PASS。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/src/config.rs src-tauri/src/lib.rs
git commit -m "feat: local app config with generated device id"
```

---

### Task 7: git CLI 封装 gitcli.rs

**Files:**
- Create: `src-tauri/src/gitcli.rs`
- Modify: `src-tauri/src/lib.rs`（加 `pub mod gitcli;`）

测试策略：不碰网络。用 `git init --bare` 在 tempdir 里造一个"远端"，clone 用 `file://` 本地路径，push/pull 全走本地。生产环境换成 GitHub URL，凭据由用户已有的 git credential helper 处理。

- [ ] **Step 1: 写失败的测试**

创建 `src-tauri/src/gitcli.rs`，先只写测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::fs;

    /// 造一个本地 bare 仓库当远端，返回其路径字符串
    fn make_bare_remote(dir: &std::path::Path) -> String {
        let bare = dir.join("remote.git");
        run_git_in(dir, &["init", "--bare", "-b", "main", "remote.git"]).unwrap();
        bare.to_string_lossy().replace('\\', "/")
    }

    #[test]
    fn clone_commit_push_pull_cycle() {
        let tmp = tempfile::tempdir().unwrap();
        let remote_url = make_bare_remote(tmp.path());

        // 设备 A：clone、写文件、commit、push
        let a_dir = tmp.path().join("deviceA");
        let repo_a = GitRepo::ensure_clone(&remote_url, &a_dir).unwrap();
        fs::write(a_dir.join("hello.txt"), "v1").unwrap();
        assert!(repo_a.commit_all("add hello").unwrap()); // true = 有东西可提交
        repo_a.push().unwrap();

        // 空提交返回 false，且不报错
        assert!(!repo_a.commit_all("nothing").unwrap());

        // 设备 B：clone 后能看到文件
        let b_dir = tmp.path().join("deviceB");
        let repo_b = GitRepo::ensure_clone(&remote_url, &b_dir).unwrap();
        assert_eq!(fs::read_to_string(b_dir.join("hello.txt")).unwrap(), "v1");

        // A 改文件推送，B pull 后可见
        fs::write(a_dir.join("hello.txt"), "v2").unwrap();
        repo_a.commit_all("update").unwrap();
        repo_a.push().unwrap();
        repo_b.pull_rebase().unwrap();
        assert_eq!(fs::read_to_string(b_dir.join("hello.txt")).unwrap(), "v2");

        // ensure_clone 对已存在的 clone 直接复用，不报错
        let again = GitRepo::ensure_clone(&remote_url, &a_dir).unwrap();
        assert_eq!(again.dir, a_dir);
    }

    #[test]
    fn empty_remote_clone_then_first_push() {
        // 全新 bare 远端没有任何提交，clone 出来是空工作区，首推要能成功
        let tmp = tempfile::tempdir().unwrap();
        let remote_url = make_bare_remote(tmp.path());
        let dir = tmp.path().join("w");
        let repo = GitRepo::ensure_clone(&remote_url, &dir).unwrap();
        fs::write(dir.join("a.txt"), "x").unwrap();
        repo.commit_all("first").unwrap();
        repo.push().unwrap();
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test gitcli`
Expected: FAIL — `GitRepo` / `run_git_in` 未定义。

- [ ] **Step 3: 实现**

在测试模块上方写：

```rust
use anyhow::{bail, Context, Result};
use std::path::{Path, PathBuf};
use std::process::Command;

/// 在指定目录执行 git，返回 stdout；非零退出码报错并带上 stderr
pub fn run_git_in(dir: &Path, args: &[&str]) -> Result<String> {
    let output = Command::new("git")
        .current_dir(dir)
        // 测试与生产都不依赖全局身份配置
        .args(["-c", "user.email=ai-session-manager@local", "-c", "user.name=ai-session-manager"])
        .args(args)
        .output()
        .context("failed to spawn git — is git installed and on PATH?")?;
    if !output.status.success() {
        bail!(
            "git {:?} failed: {}",
            args,
            String::from_utf8_lossy(&output.stderr)
        );
    }
    Ok(String::from_utf8_lossy(&output.stdout).into_owned())
}

pub struct GitRepo {
    pub dir: PathBuf,
}

impl GitRepo {
    /// dest 已是 clone 则直接打开，否则 clone url 到 dest
    pub fn ensure_clone(url: &str, dest: &Path) -> Result<GitRepo> {
        if dest.join(".git").exists() {
            return Ok(GitRepo { dir: dest.to_path_buf() });
        }
        let parent = dest.parent().context("dest has no parent")?;
        std::fs::create_dir_all(parent)?;
        let dest_name = dest.file_name().context("bad dest")?.to_string_lossy();
        run_git_in(parent, &["clone", url, &dest_name])?;
        let repo = GitRepo { dir: dest.to_path_buf() };
        // 空远端 clone 出来可能没有分支，确保本地在 main 上
        let _ = run_git_in(&repo.dir, &["checkout", "-B", "main"]);
        Ok(repo)
    }

    /// add -A 并 commit。返回 true=产生了提交，false=没有变更。
    pub fn commit_all(&self, message: &str) -> Result<bool> {
        run_git_in(&self.dir, &["add", "-A"])?;
        let status = run_git_in(&self.dir, &["status", "--porcelain"])?;
        if status.trim().is_empty() {
            return Ok(false);
        }
        run_git_in(&self.dir, &["commit", "-m", message])?;
        Ok(true)
    }

    pub fn push(&self) -> Result<()> {
        run_git_in(&self.dir, &["push", "-u", "origin", "main"])?;
        Ok(())
    }

    /// 拉取远端。远端还没有 main（全新仓库）时静默成功。
    pub fn pull_rebase(&self) -> Result<()> {
        match run_git_in(&self.dir, &["pull", "--rebase", "origin", "main"]) {
            Ok(_) => Ok(()),
            Err(e) => {
                let msg = e.to_string();
                if msg.contains("couldn't find remote ref") {
                    Ok(()) // 远端空仓库，无可拉取
                } else {
                    Err(e)
                }
            }
        }
    }
}
```

`lib.rs` 加 `pub mod gitcli;`。

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test gitcli`
Expected: 2 个测试 PASS。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/src/gitcli.rs src-tauri/src/lib.rs
git commit -m "feat: git cli wrapper tested against local bare remote"
```

---

### Task 8: 同步仓库布局 syncrepo.rs

**Files:**
- Create: `src-tauri/src/syncrepo.rs`
- Modify: `src-tauri/src/lib.rs`（加 `pub mod syncrepo;`）

职责：在一个已 clone 的工作区目录上读写 `sessions/<id>/{transcript.jsonl,meta.json}` 和 `devices/<id>.json`。不做任何 git 操作（那是 Task 9 的事）。meta 写入遵守 LWW：磁盘上已有更新的 `updated_at` 时拒绝覆盖。

- [ ] **Step 1: 写失败的测试**

创建 `src-tauri/src/syncrepo.rs`，先只写测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::model::{SessionMeta, SessionSource};
    use std::fs;

    fn sample_meta(updated_at: &str) -> SessionMeta {
        SessionMeta {
            schema_version: 1,
            session_id: "s-1".into(),
            source: SessionSource::Codex,
            title: "t".into(),
            description: String::new(),
            tags: vec![],
            starred: false,
            origin_device_id: "dev-a".into(),
            origin_cwd: r"E:\fake\proj".into(),
            relative_store_path: "2026/06/21/rollout-x.jsonl".into(),
            started_at: "2026-06-21T02:54:52.130Z".into(),
            last_active_at: "2026-06-21T02:55:10.000Z".into(),
            updated_at: updated_at.into(),
        }
    }

    #[test]
    fn write_session_copies_transcript_and_meta() {
        let tmp = tempfile::tempdir().unwrap();
        let src = tmp.path().join("orig.jsonl");
        fs::write(&src, "{\"line\":1}\n").unwrap();

        let repo = SyncRepo { root: tmp.path().join("repo") };
        repo.write_session(&src, &sample_meta("2026-07-05T00:00:00Z")).unwrap();

        let dir = repo.root.join("sessions/s-1");
        assert_eq!(fs::read_to_string(dir.join("transcript.jsonl")).unwrap(), "{\"line\":1}\n");
        let metas = repo.list_metas().unwrap();
        assert_eq!(metas.len(), 1);
        assert_eq!(metas[0].session_id, "s-1");
    }

    #[test]
    fn meta_lww_older_write_is_rejected_newer_wins() {
        let tmp = tempfile::tempdir().unwrap();
        let repo = SyncRepo { root: tmp.path().to_path_buf() };

        repo.write_meta(&sample_meta("2026-07-05T10:00:00Z")).unwrap();
        // 更旧的写入被忽略
        let mut older = sample_meta("2026-07-05T09:00:00Z");
        older.title = "旧标题".into();
        repo.write_meta(&older).unwrap();
        assert_eq!(repo.read_meta("s-1").unwrap().title, "t");
        // 更新的写入生效
        let mut newer = sample_meta("2026-07-05T11:00:00Z");
        newer.title = "新标题".into();
        repo.write_meta(&newer).unwrap();
        assert_eq!(repo.read_meta("s-1").unwrap().title, "新标题");
    }

    #[test]
    fn device_info_round_trip() {
        let tmp = tempfile::tempdir().unwrap();
        let repo = SyncRepo { root: tmp.path().to_path_buf() };
        let info = crate::model::DeviceInfo {
            device_id: "dev-a".into(),
            hostname: "PC-A".into(),
            os: "windows".into(),
            path_mappings: vec![crate::paths::PathMapping {
                from_prefix: "/Users/x/code".into(),
                to_prefix: r"E:\fromGithub".into(),
            }],
            last_sync_at: "2026-07-05T00:00:00Z".into(),
        };
        repo.write_device_info(&info).unwrap();
        let back = repo.read_device_info("dev-a").unwrap().unwrap();
        assert_eq!(back.path_mappings.len(), 1);
        assert!(repo.read_device_info("dev-zzz").unwrap().is_none());
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test syncrepo`
Expected: FAIL — `SyncRepo` 未定义。

- [ ] **Step 3: 实现**

在测试模块上方写：

```rust
use crate::model::{DeviceInfo, SessionMeta};
use anyhow::{Context, Result};
use std::fs;
use std::path::{Path, PathBuf};

pub struct SyncRepo {
    /// git clone 的工作区根目录
    pub root: PathBuf,
}

impl SyncRepo {
    fn session_dir(&self, session_id: &str) -> PathBuf {
        self.root.join("sessions").join(session_id)
    }

    /// 拷贝 transcript 并写 meta（meta 遵守 LWW）
    pub fn write_session(&self, transcript_src: &Path, meta: &SessionMeta) -> Result<()> {
        let dir = self.session_dir(&meta.session_id);
        fs::create_dir_all(&dir)?;
        fs::copy(transcript_src, dir.join("transcript.jsonl"))
            .with_context(|| format!("copy {}", transcript_src.display()))?;
        self.write_meta(meta)
    }

    /// LWW：磁盘上已有 updated_at 更新（或相等）的 meta 时本次写入被忽略
    pub fn write_meta(&self, meta: &SessionMeta) -> Result<()> {
        let dir = self.session_dir(&meta.session_id);
        fs::create_dir_all(&dir)?;
        let path = dir.join("meta.json");
        if path.exists() {
            if let Ok(existing) = self.read_meta(&meta.session_id) {
                // ISO8601 UTC 字符串可直接按字典序比较
                if existing.updated_at.as_str() >= meta.updated_at.as_str() {
                    return Ok(());
                }
            }
        }
        fs::write(&path, serde_json::to_string_pretty(meta)?)?;
        Ok(())
    }

    pub fn read_meta(&self, session_id: &str) -> Result<SessionMeta> {
        let text = fs::read_to_string(self.session_dir(session_id).join("meta.json"))?;
        Ok(serde_json::from_str(&text)?)
    }

    pub fn transcript_path(&self, session_id: &str) -> PathBuf {
        self.session_dir(session_id).join("transcript.jsonl")
    }

    /// 列出仓库中所有会话的 meta（坏文件跳过）
    pub fn list_metas(&self) -> Result<Vec<SessionMeta>> {
        let mut out = Vec::new();
        let sessions = self.root.join("sessions");
        let Ok(entries) = fs::read_dir(&sessions) else { return Ok(out) };
        for e in entries.flatten() {
            if let Some(id) = e.file_name().to_str() {
                if let Ok(m) = self.read_meta(id) {
                    out.push(m);
                }
            }
        }
        out.sort_by(|a, b| b.last_active_at.cmp(&a.last_active_at));
        Ok(out)
    }

    pub fn write_device_info(&self, info: &DeviceInfo) -> Result<()> {
        let dir = self.root.join("devices");
        fs::create_dir_all(&dir)?;
        fs::write(
            dir.join(format!("{}.json", info.device_id)),
            serde_json::to_string_pretty(info)?,
        )?;
        Ok(())
    }

    pub fn read_device_info(&self, device_id: &str) -> Result<Option<DeviceInfo>> {
        let path = self.root.join("devices").join(format!("{}.json", device_id));
        if !path.exists() {
            return Ok(None);
        }
        let text = fs::read_to_string(path)?;
        Ok(Some(serde_json::from_str(&text)?))
    }
}
```

`lib.rs` 加 `pub mod syncrepo;`。

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test syncrepo`
Expected: 3 个测试 PASS。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/src/syncrepo.rs src-tauri/src/lib.rs
git commit -m "feat: sync repo layout with meta last-write-wins"
```

---

### Task 9: 同步编排 sync.rs

**Files:**
- Create: `src-tauri/src/sync.rs`
- Modify: `src-tauri/src/lib.rs`（加 `pub mod sync;`）

职责：把 gitcli + syncrepo 串成两个动作：`refresh()`（pull）和 `upload()`（pull → 写文件 → commit → push）。上传时对已存在的会话只在本地 transcript 更新时覆盖（按字节数判断：本地更长说明追加了新消息；相同跳过）。meta 只在首次上传时创建，之后 meta 的修改走 `update_meta`。

- [ ] **Step 1: 写失败的测试**

创建 `src-tauri/src/sync.rs`，先只写测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::gitcli::{run_git_in, GitRepo};
    use crate::model::{SessionSource, SessionSummary};
    use std::fs;

    fn make_bare_remote(dir: &std::path::Path) -> String {
        run_git_in(dir, &["init", "--bare", "-b", "main", "remote.git"]).unwrap();
        dir.join("remote.git").to_string_lossy().replace('\\', "/")
    }

    fn sample_summary(transcript: &std::path::Path) -> SessionSummary {
        SessionSummary {
            session_id: "s-1".into(),
            source: SessionSource::ClaudeCode,
            cwd: r"E:\fake\proj".into(),
            transcript_path: transcript.to_string_lossy().into_owned(),
            relative_store_path: "E--fake-proj/s-1.jsonl".into(),
            first_message: "帮我修 bug".into(),
            message_count: 3,
            started_at: "2026-07-05T02:46:20.453Z".into(),
            last_active_at: "2026-07-05T02:50:00.000Z".into(),
        }
    }

    #[test]
    fn upload_then_second_device_sees_session() {
        let tmp = tempfile::tempdir().unwrap();
        let remote = make_bare_remote(tmp.path());
        let transcript = tmp.path().join("orig.jsonl");
        fs::write(&transcript, "{\"a\":1}\n").unwrap();

        // 设备 A 上传
        let engine_a = SyncEngine::connect(&remote, &tmp.path().join("mirrorA")).unwrap();
        let n = engine_a
            .upload(&[sample_summary(&transcript)], "dev-a", "2026-07-05T10:00:00Z")
            .unwrap();
        assert_eq!(n, 1);

        // 设备 B refresh 后能列出
        let engine_b = SyncEngine::connect(&remote, &tmp.path().join("mirrorB")).unwrap();
        engine_b.refresh().unwrap();
        let metas = engine_b.repo().list_metas().unwrap();
        assert_eq!(metas.len(), 1);
        assert_eq!(metas[0].title, "帮我修 bug");
        assert_eq!(metas[0].origin_device_id, "dev-a");
    }

    #[test]
    fn reupload_longer_transcript_overwrites_but_keeps_meta_edits() {
        let tmp = tempfile::tempdir().unwrap();
        let remote = make_bare_remote(tmp.path());
        let transcript = tmp.path().join("orig.jsonl");
        fs::write(&transcript, "{\"a\":1}\n").unwrap();

        let engine = SyncEngine::connect(&remote, &tmp.path().join("mirror")).unwrap();
        engine.upload(&[sample_summary(&transcript)], "dev-a", "2026-07-05T10:00:00Z").unwrap();

        // 用户改了标题（updated_at 更新）
        let mut meta = engine.repo().read_meta("s-1").unwrap();
        meta.title = "我的重要会话".into();
        meta.updated_at = "2026-07-05T11:00:00Z".into();
        engine.repo().write_meta(&meta).unwrap();

        // 会话又聊了几句（文件变长），再次上传
        fs::write(&transcript, "{\"a\":1}\n{\"a\":2}\n").unwrap();
        let n = engine.upload(&[sample_summary(&transcript)], "dev-a", "2026-07-05T12:00:00Z").unwrap();
        assert_eq!(n, 1);

        // transcript 更新了，但标题没有被 from_summary 的默认值冲掉
        let text = fs::read_to_string(engine.repo().transcript_path("s-1")).unwrap();
        assert!(text.contains("{\"a\":2}"));
        assert_eq!(engine.repo().read_meta("s-1").unwrap().title, "我的重要会话");
    }

    #[test]
    fn upload_unchanged_transcript_is_skipped() {
        let tmp = tempfile::tempdir().unwrap();
        let remote = make_bare_remote(tmp.path());
        let transcript = tmp.path().join("orig.jsonl");
        fs::write(&transcript, "{\"a\":1}\n").unwrap();

        let engine = SyncEngine::connect(&remote, &tmp.path().join("mirror")).unwrap();
        engine.upload(&[sample_summary(&transcript)], "dev-a", "2026-07-05T10:00:00Z").unwrap();
        let n = engine.upload(&[sample_summary(&transcript)], "dev-a", "2026-07-05T10:05:00Z").unwrap();
        assert_eq!(n, 0); // 没有变化，什么都不用推
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test sync::`
Expected: FAIL — `SyncEngine` 未定义。

- [ ] **Step 3: 实现**

在测试模块上方写：

```rust
use crate::gitcli::GitRepo;
use crate::model::{SessionMeta, SessionSummary};
use crate::syncrepo::SyncRepo;
use anyhow::Result;
use std::fs;
use std::path::Path;

pub struct SyncEngine {
    git: GitRepo,
    repo: SyncRepo,
}

impl SyncEngine {
    /// clone（或复用）远端到 mirror_dir
    pub fn connect(remote_url: &str, mirror_dir: &Path) -> Result<SyncEngine> {
        let git = GitRepo::ensure_clone(remote_url, mirror_dir)?;
        let repo = SyncRepo { root: git.dir.clone() };
        Ok(SyncEngine { git, repo })
    }

    pub fn repo(&self) -> &SyncRepo {
        &self.repo
    }

    /// pull 远端最新
    pub fn refresh(&self) -> Result<()> {
        self.git.pull_rebase()
    }

    /// 上传选中的本机会话。返回实际写入的数量。
    /// 规则：transcript 内容与仓库中不同才覆盖；meta 只在不存在时创建。
    pub fn upload(
        &self,
        sessions: &[SessionSummary],
        device_id: &str,
        now_iso: &str,
    ) -> Result<usize> {
        self.refresh()?;
        let mut written = 0usize;
        for s in sessions {
            let repo_transcript = self.repo.transcript_path(&s.session_id);
            let local_bytes = fs::read(&s.transcript_path)?;
            let unchanged = repo_transcript.exists()
                && fs::read(&repo_transcript).map(|b| b == local_bytes).unwrap_or(false);
            let meta_exists = self.repo.read_meta(&s.session_id).is_ok();
            if unchanged && meta_exists {
                continue;
            }
            if !meta_exists {
                let meta = SessionMeta::from_summary(s, device_id, now_iso);
                self.repo.write_session(Path::new(&s.transcript_path), &meta)?;
            } else {
                // meta 已存在：只更新 transcript 和 last_active_at，不动用户编辑过的字段
                fs::create_dir_all(repo_transcript.parent().unwrap())?;
                fs::write(&repo_transcript, &local_bytes)?;
                let mut meta = self.repo.read_meta(&s.session_id)?;
                meta.last_active_at = s.last_active_at.clone();
                meta.updated_at = now_iso.to_string();
                self.repo.write_meta(&meta)?;
            }
            written += 1;
        }
        if written > 0 {
            let msg = format!("sync: upload {} session(s) from {}", written, device_id);
            if self.git.commit_all(&msg)? {
                self.git.push()?;
            }
        }
        Ok(written)
    }

    /// 修改元数据并推送（rename/description/tags/star 都走这里）
    pub fn update_meta(&self, meta: &SessionMeta) -> Result<()> {
        self.refresh()?;
        self.repo.write_meta(meta)?;
        if self.git.commit_all(&format!("meta: update {}", meta.session_id))? {
            self.git.push()?;
        }
        Ok(())
    }
}
```

`lib.rs` 加 `pub mod sync;`。

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test sync::`
Expected: 3 个测试 PASS。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/src/sync.rs src-tauri/src/lib.rs
git commit -m "feat: sync engine upload/refresh/update-meta orchestration"
```

---

### Task 10: 路径重映射恢复 restore.rs

**Files:**
- Create: `src-tauri/src/restore.rs`
- Modify: `src-tauri/src/lib.rs`（加 `pub mod restore;`）

职责：把仓库中的 transcript 恢复到本机对应工具的目录，重写路径字段。
- Claude Code：目标文件 `<claude_home>/projects/<munge(target_cwd)>/<session_id>.jsonl`，每行 JSON 若含顶层 `cwd` 字段则改为 target_cwd，其余字段原样（重新序列化允许键序变化）。
- Codex：目标文件 `<codex_home>/sessions/<relative_store_path>`（保留原日期目录），`session_meta.payload.cwd` 与每个 `turn_context.payload.cwd`/`workspace_roots` 改写。
- 目标文件已存在时**拒绝覆盖**（返回错误），避免覆盖本机正在进行的同名会话。

- [ ] **Step 1: 写失败的测试**

创建 `src-tauri/src/restore.rs`，先只写测试（复用 Task 4/5 的 fixtures）：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::model::{SessionMeta, SessionSource};
    use serde_json::Value;
    use std::fs;
    use std::path::PathBuf;

    fn fixture(rel: &str) -> PathBuf {
        PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures").join(rel)
    }

    fn claude_meta() -> SessionMeta {
        SessionMeta {
            schema_version: 1,
            session_id: "11111111-aaaa-bbbb-cccc-000000000001".into(),
            source: SessionSource::ClaudeCode,
            title: "t".into(),
            description: String::new(),
            tags: vec![],
            starred: false,
            origin_device_id: "dev-a".into(),
            origin_cwd: r"E:\fake\proj".into(),
            relative_store_path: "E--fake-proj/11111111-aaaa-bbbb-cccc-000000000001.jsonl".into(),
            started_at: "".into(),
            last_active_at: "".into(),
            updated_at: "2026-07-05T00:00:00Z".into(),
        }
    }

    #[test]
    fn restore_claude_rewrites_cwd_and_munges_dir() {
        let tmp = tempfile::tempdir().unwrap();
        let claude_home = tmp.path().join(".claude");
        let src = fixture("claude/E--fake-proj/11111111-aaaa-bbbb-cccc-000000000001.jsonl");

        let out = restore_claude(&src, &claude_meta(), "/Users/sybil/code/proj", &claude_home).unwrap();
        assert!(out.ends_with(
            std::path::Path::new("projects/-Users-sybil-code-proj/11111111-aaaa-bbbb-cccc-000000000001.jsonl")
        ));

        let text = fs::read_to_string(&out).unwrap();
        let mut saw_message = false;
        for line in text.lines() {
            let v: Value = serde_json::from_str(line).unwrap();
            if let Some(cwd) = v.get("cwd") {
                assert_eq!(cwd.as_str().unwrap(), "/Users/sybil/code/proj");
                saw_message = true;
            }
            // 其他字段保留
            if v.get("uuid").and_then(Value::as_str) == Some("u-1") {
                assert_eq!(v["message"]["content"].as_str().unwrap(), "帮我修 bug");
            }
        }
        assert!(saw_message);
    }

    #[test]
    fn restore_claude_refuses_to_overwrite() {
        let tmp = tempfile::tempdir().unwrap();
        let claude_home = tmp.path().join(".claude");
        let src = fixture("claude/E--fake-proj/11111111-aaaa-bbbb-cccc-000000000001.jsonl");
        restore_claude(&src, &claude_meta(), "/x", &claude_home).unwrap();
        assert!(restore_claude(&src, &claude_meta(), "/x", &claude_home).is_err());
    }

    #[test]
    fn restore_codex_rewrites_session_meta_and_turn_context() {
        let tmp = tempfile::tempdir().unwrap();
        let codex_home = tmp.path().join(".codex");
        let src = fixture(
            "codex/2026/06/21/rollout-2026-06-21T10-54-52-22222222-aaaa-bbbb-cccc-000000000002.jsonl",
        );
        let mut meta = claude_meta();
        meta.session_id = "22222222-aaaa-bbbb-cccc-000000000002".into();
        meta.source = SessionSource::Codex;
        meta.relative_store_path =
            "2026/06/21/rollout-2026-06-21T10-54-52-22222222-aaaa-bbbb-cccc-000000000002.jsonl".into();

        let out = restore_codex(&src, &meta, "/Users/sybil/code/proj", &codex_home).unwrap();
        assert!(out.to_string_lossy().replace('\\', "/").ends_with(
            "sessions/2026/06/21/rollout-2026-06-21T10-54-52-22222222-aaaa-bbbb-cccc-000000000002.jsonl"
        ));

        let text = fs::read_to_string(&out).unwrap();
        for line in text.lines() {
            let v: Value = serde_json::from_str(line).unwrap();
            match v.get("type").and_then(Value::as_str) {
                Some("session_meta") => {
                    assert_eq!(v["payload"]["cwd"].as_str().unwrap(), "/Users/sybil/code/proj");
                }
                Some("turn_context") => {
                    assert_eq!(v["payload"]["cwd"].as_str().unwrap(), "/Users/sybil/code/proj");
                    assert_eq!(
                        v["payload"]["workspace_roots"][0].as_str().unwrap(),
                        "/Users/sybil/code/proj"
                    );
                }
                _ => {}
            }
        }
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test restore`
Expected: FAIL — 函数未定义。

- [ ] **Step 3: 实现**

在测试模块上方写：

```rust
use crate::model::SessionMeta;
use crate::paths::munge_claude_project_dir;
use anyhow::{bail, Context, Result};
use serde_json::{json, Value};
use std::fs;
use std::path::{Path, PathBuf};

fn write_lines(path: &Path, lines: &[String]) -> Result<()> {
    fs::create_dir_all(path.parent().context("no parent")?)?;
    fs::write(path, lines.join("\n") + "\n")?;
    Ok(())
}

/// 恢复 Claude Code 会话。src 是仓库里的 transcript.jsonl。
/// 返回写入的目标路径。
pub fn restore_claude(
    src: &Path,
    meta: &SessionMeta,
    target_cwd: &str,
    claude_home: &Path,
) -> Result<PathBuf> {
    let dest = claude_home
        .join("projects")
        .join(munge_claude_project_dir(target_cwd))
        .join(format!("{}.jsonl", meta.session_id));
    if dest.exists() {
        bail!("target already exists: {}", dest.display());
    }

    let text = fs::read_to_string(src)?;
    let mut out = Vec::new();
    for line in text.lines() {
        if line.trim().is_empty() {
            continue;
        }
        match serde_json::from_str::<Value>(line) {
            Ok(mut v) => {
                if v.get("cwd").is_some() {
                    v["cwd"] = json!(target_cwd);
                }
                out.push(serde_json::to_string(&v)?);
            }
            Err(_) => out.push(line.to_string()), // 解析不了的行原样保留
        }
    }
    write_lines(&dest, &out)?;
    Ok(dest)
}

/// 恢复 Codex 会话到原日期目录（relative_store_path）。
pub fn restore_codex(
    src: &Path,
    meta: &SessionMeta,
    target_cwd: &str,
    codex_home: &Path,
) -> Result<PathBuf> {
    let dest = codex_home.join("sessions").join(&meta.relative_store_path);
    if dest.exists() {
        bail!("target already exists: {}", dest.display());
    }

    let text = fs::read_to_string(src)?;
    let mut out = Vec::new();
    for line in text.lines() {
        if line.trim().is_empty() {
            continue;
        }
        match serde_json::from_str::<Value>(line) {
            Ok(mut v) => {
                let typ = v.get("type").and_then(Value::as_str).unwrap_or("").to_string();
                if typ == "session_meta" || typ == "turn_context" {
                    if v["payload"].get("cwd").is_some() {
                        v["payload"]["cwd"] = json!(target_cwd);
                    }
                    if v["payload"].get("workspace_roots").is_some() {
                        v["payload"]["workspace_roots"] = json!([target_cwd]);
                    }
                }
                out.push(serde_json::to_string(&v)?);
            }
            Err(_) => out.push(line.to_string()),
        }
    }
    write_lines(&dest, &out)?;
    Ok(dest)
}
```

`lib.rs` 加 `pub mod restore;`。

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test restore`
Expected: 3 个测试 PASS。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/src/restore.rs src-tauri/src/lib.rs
git commit -m "feat: restore sessions with path remapping, refuse overwrite"
```

---

### Task 11: Tauri command 层（lib.rs）

**Files:**
- Modify: `src-tauri/src/lib.rs`
- Modify: `src-tauri/Cargo.toml`（加 chrono）

command 层是薄封装：组装路径 → 调核心模块 → `anyhow::Error` 转 `String`。逻辑已被模块测试覆盖，本任务验收标准是编译通过 + Task 12 的端到端测试。

- [ ] **Step 1: 加 chrono 依赖**

`src-tauri/Cargo.toml` `[dependencies]` 加：

```toml
chrono = "0.4"
```

- [ ] **Step 2: 实现 commands**

`src-tauri/src/lib.rs` 全文替换为（保留模板生成的 `run()` 结构，替换 invoke_handler；模块声明已在前面任务加好）：

```rust
pub mod config;
pub mod gitcli;
pub mod model;
pub mod paths;
pub mod restore;
pub mod scan_claude;
pub mod scan_codex;
pub mod sync;
pub mod syncrepo;

use model::{DeviceInfo, SessionMeta, SessionSummary};
use paths::PathMapping;
use serde::{Deserialize, Serialize};
use std::path::PathBuf;
use sync::SyncEngine;
use tauri::Manager;

fn now_iso() -> String {
    chrono::Utc::now().format("%Y-%m-%dT%H:%M:%S%.3fZ").to_string()
}

fn hostname() -> String {
    std::env::var("COMPUTERNAME")
        .or_else(|_| std::env::var("HOSTNAME"))
        .unwrap_or_else(|_| "unknown".into())
}

struct AppDirs {
    config_dir: PathBuf,
    mirror_dir: PathBuf,
    claude_projects: PathBuf,
    codex_sessions: PathBuf,
}

fn app_dirs(app: &tauri::AppHandle) -> Result<AppDirs, String> {
    let home = dirs::home_dir().ok_or("no home dir")?;
    Ok(AppDirs {
        config_dir: app.path().app_config_dir().map_err(|e| e.to_string())?,
        mirror_dir: app
            .path()
            .app_data_dir()
            .map_err(|e| e.to_string())?
            .join("mirror"),
        claude_projects: home.join(".claude").join("projects"),
        codex_sessions: home.join(".codex").join("sessions"),
    })
}

fn engine(app: &tauri::AppHandle) -> Result<(SyncEngine, config::AppConfig), String> {
    let dirs = app_dirs(app)?;
    let cfg = config::AppConfig::load_or_init(&dirs.config_dir).map_err(|e| e.to_string())?;
    let url = cfg.repo_url.clone().ok_or("repo_url not configured")?;
    let eng = SyncEngine::connect(&url, &dirs.mirror_dir).map_err(|e| e.to_string())?;
    Ok((eng, cfg))
}

#[derive(Serialize, Deserialize)]
struct Settings {
    device_id: String,
    repo_url: Option<String>,
    path_mappings: Vec<PathMapping>,
}

#[tauri::command]
fn list_local_sessions(app: tauri::AppHandle) -> Result<Vec<SessionSummary>, String> {
    let dirs = app_dirs(&app)?;
    let mut out = scan_claude::scan(&dirs.claude_projects).map_err(|e| e.to_string())?;
    out.extend(scan_codex::scan(&dirs.codex_sessions).map_err(|e| e.to_string())?);
    out.sort_by(|a, b| b.last_active_at.cmp(&a.last_active_at));
    Ok(out)
}

#[tauri::command]
fn get_settings(app: tauri::AppHandle) -> Result<Settings, String> {
    let dirs = app_dirs(&app)?;
    let cfg = config::AppConfig::load_or_init(&dirs.config_dir).map_err(|e| e.to_string())?;
    // 已连接仓库时读自己的 device 文件取映射；未连接则空
    let path_mappings = match engine(&app) {
        Ok((eng, _)) => eng
            .repo()
            .read_device_info(&cfg.device_id)
            .ok()
            .flatten()
            .map(|d| d.path_mappings)
            .unwrap_or_default(),
        Err(_) => vec![],
    };
    Ok(Settings { device_id: cfg.device_id, repo_url: cfg.repo_url, path_mappings })
}

#[tauri::command]
fn save_settings(
    app: tauri::AppHandle,
    repo_url: Option<String>,
    path_mappings: Vec<PathMapping>,
) -> Result<(), String> {
    let dirs = app_dirs(&app)?;
    let mut cfg = config::AppConfig::load_or_init(&dirs.config_dir).map_err(|e| e.to_string())?;
    cfg.repo_url = repo_url;
    cfg.save(&dirs.config_dir).map_err(|e| e.to_string())?;
    if cfg.repo_url.is_some() {
        let (eng, cfg) = engine(&app)?;
        eng.refresh().map_err(|e| e.to_string())?;
        let info = DeviceInfo {
            device_id: cfg.device_id.clone(),
            hostname: hostname(),
            os: std::env::consts::OS.into(),
            path_mappings,
            last_sync_at: now_iso(),
        };
        eng.repo().write_device_info(&info).map_err(|e| e.to_string())?;
        eng.update_meta_commit("devices: update mappings").map_err(|e| e.to_string())?;
    }
    Ok(())
}

#[tauri::command]
fn list_repo_sessions(app: tauri::AppHandle) -> Result<Vec<SessionMeta>, String> {
    let (eng, _) = engine(&app)?;
    eng.refresh().map_err(|e| e.to_string())?;
    eng.repo().list_metas().map_err(|e| e.to_string())
}

#[tauri::command]
fn upload_sessions(app: tauri::AppHandle, session_ids: Vec<String>) -> Result<usize, String> {
    let (eng, cfg) = engine(&app)?;
    let all = list_local_sessions(app.clone())?;
    let selected: Vec<_> = all
        .into_iter()
        .filter(|s| session_ids.contains(&s.session_id))
        .collect();
    eng.upload(&selected, &cfg.device_id, &now_iso()).map_err(|e| e.to_string())
}

#[tauri::command]
fn update_session_meta(app: tauri::AppHandle, mut meta: SessionMeta) -> Result<(), String> {
    let (eng, _) = engine(&app)?;
    meta.updated_at = now_iso(); // updated_at 由后端统一盖章
    eng.update_meta(&meta).map_err(|e| e.to_string())
}

#[tauri::command]
fn suggest_restore_path(app: tauri::AppHandle, session_id: String) -> Result<Option<String>, String> {
    let (eng, cfg) = engine(&app)?;
    let meta = eng.repo().read_meta(&session_id).map_err(|e| e.to_string())?;
    let mappings = eng
        .repo()
        .read_device_info(&cfg.device_id)
        .ok()
        .flatten()
        .map(|d| d.path_mappings)
        .unwrap_or_default();
    Ok(paths::remap_path(&meta.origin_cwd, &mappings))
}

#[tauri::command]
fn restore_session(
    app: tauri::AppHandle,
    session_id: String,
    target_cwd: String,
) -> Result<String, String> {
    let (eng, _) = engine(&app)?;
    eng.refresh().map_err(|e| e.to_string())?;
    let meta = eng.repo().read_meta(&session_id).map_err(|e| e.to_string())?;
    let src = eng.repo().transcript_path(&session_id);
    let home = dirs::home_dir().ok_or("no home dir")?;
    let dest = match meta.source {
        model::SessionSource::ClaudeCode => {
            restore::restore_claude(&src, &meta, &target_cwd, &home.join(".claude"))
        }
        model::SessionSource::Codex => {
            restore::restore_codex(&src, &meta, &target_cwd, &home.join(".codex"))
        }
    }
    .map_err(|e| e.to_string())?;
    Ok(dest.to_string_lossy().into_owned())
}

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .invoke_handler(tauri::generate_handler![
            list_local_sessions,
            get_settings,
            save_settings,
            list_repo_sessions,
            upload_sessions,
            update_session_meta,
            suggest_restore_path,
            restore_session
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

注意两点：
1. `save_settings` 里用了 `eng.update_meta_commit(...)`——`sync.rs` 还没有这个方法，在 `SyncEngine` 上补一个通用的提交推送方法：

```rust
    /// 提交并推送当前工作区所有变更（devices 文件等非 meta 写入用）
    pub fn update_meta_commit(&self, message: &str) -> Result<()> {
        if self.git.commit_all(message)? {
            self.git.push()?;
        }
        Ok(())
    }
```

2. 模板 `lib.rs` 里原有的 `greet` 示例 command 删除；如果模板没有 `tauri_plugin_opener` 就去掉那一行 `.plugin(...)`（以模板实际生成为准）。

- [ ] **Step 3: 编译验证**

Run: `cargo check`（在 `src-tauri/` 下）
Expected: 编译通过。随后 `cargo test` 全量跑，之前所有模块测试仍 PASS。

- [ ] **Step 4: Commit**

```powershell
git add src-tauri/src/lib.rs src-tauri/src/sync.rs src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -m "feat: tauri command layer wiring core modules"
```

---

### Task 12: 双设备端到端集成测试

**Files:**
- Create: `src-tauri/tests/e2e_two_devices.rs`

模拟完整用户旅程：设备 A 扫描 fixture 会话 → 上传到本地 bare"远端" → 设备 B 拉取 → 按路径映射恢复 → 断言文件落在正确位置且 cwd 已重写。

注意：集成测试以库 crate 方式引用代码。crate 名以 `src-tauri/Cargo.toml` 的 `[lib] name` 为准（Tauri 模板通常是 `<项目名下划线>_lib`，如 `ai_session_manager_lib`）——**执行前先打开 Cargo.toml 确认**，下面代码按 `ai_session_manager_lib` 写。

- [ ] **Step 1: 写测试（这次直接写完整场景，红→绿看跑通）**

创建 `src-tauri/tests/e2e_two_devices.rs`：

```rust
use ai_session_manager_lib::gitcli::run_git_in;
use ai_session_manager_lib::model::SessionSource;
use ai_session_manager_lib::paths::{remap_path, PathMapping};
use ai_session_manager_lib::restore::{restore_claude, restore_codex};
use ai_session_manager_lib::scan_claude;
use ai_session_manager_lib::scan_codex;
use ai_session_manager_lib::sync::SyncEngine;
use std::fs;
use std::path::PathBuf;

fn fixtures() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures")
}

#[test]
fn two_device_upload_and_restore_round_trip() {
    let tmp = tempfile::tempdir().unwrap();

    // ---- 远端：本地 bare 仓库 ----
    run_git_in(tmp.path(), &["init", "--bare", "-b", "main", "remote.git"]).unwrap();
    let remote = tmp.path().join("remote.git").to_string_lossy().replace('\\', "/");

    // ---- 设备 A：扫描 fixtures（1 个 claude + 1 个 codex），全部上传 ----
    let mut sessions = scan_claude::scan(&fixtures().join("claude")).unwrap();
    sessions.extend(scan_codex::scan(&fixtures().join("codex")).unwrap());
    assert_eq!(sessions.len(), 2);

    let engine_a = SyncEngine::connect(&remote, &tmp.path().join("mirrorA")).unwrap();
    let n = engine_a.upload(&sessions, "dev-a", "2026-07-05T10:00:00.000Z").unwrap();
    assert_eq!(n, 2);

    // 设备 A 给 claude 会话改名
    let claude_id = "11111111-aaaa-bbbb-cccc-000000000001";
    let mut meta = engine_a.repo().read_meta(claude_id).unwrap();
    meta.title = "登录 bug 修复".into();
    meta.starred = true;
    meta.updated_at = "2026-07-05T10:30:00.000Z".into();
    engine_a.update_meta(&meta).unwrap();

    // ---- 设备 B：拉取，看到 2 个会话和改过的名字 ----
    let engine_b = SyncEngine::connect(&remote, &tmp.path().join("mirrorB")).unwrap();
    engine_b.refresh().unwrap();
    let metas = engine_b.repo().list_metas().unwrap();
    assert_eq!(metas.len(), 2);
    let claude_meta = metas.iter().find(|m| m.session_id == claude_id).unwrap();
    assert_eq!(claude_meta.title, "登录 bug 修复");
    assert!(claude_meta.starred);

    // ---- 设备 B：路径映射 E:\fake -> /Users/b/work，恢复两个会话 ----
    let mappings = vec![PathMapping {
        from_prefix: r"E:\fake".into(),
        to_prefix: "/Users/b/work".into(),
    }];
    let target = remap_path(&claude_meta.origin_cwd, &mappings).unwrap();
    assert_eq!(target, "/Users/b/work/proj");

    let home_b = tmp.path().join("homeB");
    let out_claude = restore_claude(
        &engine_b.repo().transcript_path(claude_id),
        claude_meta,
        &target,
        &home_b.join(".claude"),
    )
    .unwrap();
    // 落在按新 cwd 转码的项目目录下
    assert!(out_claude
        .to_string_lossy()
        .replace('\\', "/")
        .contains("/projects/-Users-b-work-proj/"));
    assert!(fs::read_to_string(&out_claude).unwrap().contains("/Users/b/work/proj"));

    let codex_meta = metas
        .iter()
        .find(|m| matches!(m.source, SessionSource::Codex))
        .unwrap();
    let out_codex = restore_codex(
        &engine_b.repo().transcript_path(&codex_meta.session_id),
        codex_meta,
        &target,
        &home_b.join(".codex"),
    )
    .unwrap();
    // 保留原日期树
    assert!(out_codex.to_string_lossy().replace('\\', "/").contains("/sessions/2026/06/21/"));

    // ---- 设备 B 上二次恢复被拒绝（不覆盖）----
    assert!(restore_claude(
        &engine_b.repo().transcript_path(claude_id),
        claude_meta,
        &target,
        &home_b.join(".claude"),
    )
    .is_err());
}
```

- [ ] **Step 2: 运行**

Run: `cargo test --test e2e_two_devices`
Expected: PASS。若 crate 名不对，编译报错 "can't find crate"——按 Cargo.toml 的 `[lib] name` 改 `use` 行。

- [ ] **Step 3: 全量回归**

Run: `cargo test`
Expected: 所有单元测试 + 集成测试 PASS。

- [ ] **Step 4: Commit**

```powershell
git add src-tauri/tests/e2e_two_devices.rs
git commit -m "test: two-device upload/rename/restore end-to-end"
```

---

### Task 13: 前端 api 封装与会话列表

**Files:**
- Create: `src/api.ts`
- Create: `src/components/SessionList.tsx`
- Modify: `src/App.tsx`（模板生成的全文替换）

前端不写单测（逻辑都在 Rust 侧），验收方式是 Step 3 的手动冒烟。

- [ ] **Step 1: 创建 api.ts**

```typescript
import { invoke } from "@tauri-apps/api/core";

export type SessionSource = "codex" | "claude-code";

export interface SessionSummary {
  session_id: string;
  source: SessionSource;
  cwd: string;
  transcript_path: string;
  relative_store_path: string;
  first_message: string;
  message_count: number;
  started_at: string;
  last_active_at: string;
}

export interface SessionMeta {
  schema_version: number;
  session_id: string;
  source: SessionSource;
  title: string;
  description: string;
  tags: string[];
  starred: boolean;
  origin_device_id: string;
  origin_cwd: string;
  relative_store_path: string;
  started_at: string;
  last_active_at: string;
  updated_at: string;
}

export interface PathMapping {
  from_prefix: string;
  to_prefix: string;
}

export interface Settings {
  device_id: string;
  repo_url: string | null;
  path_mappings: PathMapping[];
}

export const api = {
  listLocalSessions: () => invoke<SessionSummary[]>("list_local_sessions"),
  listRepoSessions: () => invoke<SessionMeta[]>("list_repo_sessions"),
  uploadSessions: (sessionIds: string[]) =>
    invoke<number>("upload_sessions", { sessionIds }),
  updateSessionMeta: (meta: SessionMeta) =>
    invoke<void>("update_session_meta", { meta }),
  suggestRestorePath: (sessionId: string) =>
    invoke<string | null>("suggest_restore_path", { sessionId }),
  restoreSession: (sessionId: string, targetCwd: string) =>
    invoke<string>("restore_session", { sessionId, targetCwd }),
  getSettings: () => invoke<Settings>("get_settings"),
  saveSettings: (repoUrl: string | null, pathMappings: PathMapping[]) =>
    invoke<void>("save_settings", { repoUrl, pathMappings }),
};
```

- [ ] **Step 2: 创建 SessionList.tsx 与 App.tsx**

`src/components/SessionList.tsx`：

```tsx
import { useState } from "react";
import { SessionMeta, SessionSummary, api } from "../api";

const badge = (source: string) => (source === "codex" ? "🟢 Codex" : "🟠 Claude");

export function LocalList({ sessions, onUploaded }: {
  sessions: SessionSummary[];
  onUploaded: () => void;
}) {
  const [selected, setSelected] = useState<Set<string>>(new Set());
  const [busy, setBusy] = useState(false);

  const toggle = (id: string) => {
    const next = new Set(selected);
    next.has(id) ? next.delete(id) : next.add(id);
    setSelected(next);
  };

  const upload = async () => {
    setBusy(true);
    try {
      const n = await api.uploadSessions([...selected]);
      alert(`已上传 ${n} 个会话`);
      setSelected(new Set());
      onUploaded();
    } catch (e) {
      alert(`上传失败：${e}`);
    } finally {
      setBusy(false);
    }
  };

  return (
    <div>
      <button disabled={selected.size === 0 || busy} onClick={upload}>
        上传选中（{selected.size}）
      </button>
      <ul className="session-list">
        {sessions.map((s) => (
          <li key={s.session_id}>
            <label>
              <input
                type="checkbox"
                checked={selected.has(s.session_id)}
                onChange={() => toggle(s.session_id)}
              />
              <span>{badge(s.source)}</span>
              <strong>{s.first_message || "(无标题)"}</strong>
              <small>
                {s.cwd} · {s.message_count} 条 · {s.last_active_at.slice(0, 16)}
              </small>
            </label>
          </li>
        ))}
      </ul>
    </div>
  );
}

export function RepoList({ metas, onChanged, onRestore }: {
  metas: SessionMeta[];
  onChanged: () => void;
  onRestore: (meta: SessionMeta) => void;
}) {
  const save = async (meta: SessionMeta, patch: Partial<SessionMeta>) => {
    try {
      await api.updateSessionMeta({ ...meta, ...patch });
      onChanged();
    } catch (e) {
      alert(`保存失败：${e}`);
    }
  };

  return (
    <ul className="session-list">
      {metas.map((m) => (
        <li key={m.session_id}>
          <button onClick={() => save(m, { starred: !m.starred })}>
            {m.starred ? "★" : "☆"}
          </button>
          <span>{badge(m.source)}</span>
          <input
            defaultValue={m.title}
            onBlur={(e) => e.target.value !== m.title && save(m, { title: e.target.value })}
          />
          <input
            placeholder="描述…"
            defaultValue={m.description}
            onBlur={(e) =>
              e.target.value !== m.description && save(m, { description: e.target.value })
            }
          />
          <small>{m.origin_cwd}</small>
          <button onClick={() => onRestore(m)}>恢复到本机</button>
        </li>
      ))}
    </ul>
  );
}
```

`src/App.tsx` 全文替换：

```tsx
import { useEffect, useState } from "react";
import { SessionMeta, SessionSummary, api } from "./api";
import { LocalList, RepoList } from "./components/SessionList";
import { RestoreDialog } from "./components/RestoreDialog";
import { SettingsPanel } from "./components/SettingsPanel";
import "./App.css";

type Tab = "local" | "repo" | "settings";

export default function App() {
  const [tab, setTab] = useState<Tab>("local");
  const [local, setLocal] = useState<SessionSummary[]>([]);
  const [repo, setRepo] = useState<SessionMeta[]>([]);
  const [restoring, setRestoring] = useState<SessionMeta | null>(null);
  const [error, setError] = useState("");

  const loadLocal = () => api.listLocalSessions().then(setLocal).catch((e) => setError(String(e)));
  const loadRepo = () => api.listRepoSessions().then(setRepo).catch((e) => setError(String(e)));

  useEffect(() => {
    loadLocal();
  }, []);
  useEffect(() => {
    if (tab === "repo") loadRepo();
  }, [tab]);

  return (
    <main>
      <nav>
        <button onClick={() => setTab("local")}>本机会话</button>
        <button onClick={() => setTab("repo")}>仓库会话</button>
        <button onClick={() => setTab("settings")}>设置</button>
      </nav>
      {error && <p className="error">{error}</p>}
      {tab === "local" && <LocalList sessions={local} onUploaded={loadLocal} />}
      {tab === "repo" && (
        <RepoList metas={repo} onChanged={loadRepo} onRestore={setRestoring} />
      )}
      {tab === "settings" && <SettingsPanel />}
      {restoring && (
        <RestoreDialog
          meta={restoring}
          onClose={() => setRestoring(null)}
          onDone={() => {
            setRestoring(null);
            loadLocal();
          }}
        />
      )}
    </main>
  );
}
```

（`RestoreDialog` / `SettingsPanel` 在 Task 14 创建；本步结束时前端暂不编译，属预期。）

- [ ] **Step 3: Commit**

```powershell
git add src/api.ts src/components/SessionList.tsx src/App.tsx
git commit -m "feat: frontend api layer and session lists"
```

---

### Task 14: 恢复对话框、设置面板与手动冒烟

**Files:**
- Create: `src/components/RestoreDialog.tsx`
- Create: `src/components/SettingsPanel.tsx`

- [ ] **Step 1: 创建 RestoreDialog.tsx**

```tsx
import { useEffect, useState } from "react";
import { SessionMeta, api } from "../api";

export function RestoreDialog({ meta, onClose, onDone }: {
  meta: SessionMeta;
  onClose: () => void;
  onDone: () => void;
}) {
  const [target, setTarget] = useState("");
  const [busy, setBusy] = useState(false);

  useEffect(() => {
    // 用设备路径映射预填目标路径，映射不到就留给用户手输
    api.suggestRestorePath(meta.session_id).then((p) => setTarget(p ?? meta.origin_cwd));
  }, [meta.session_id]);

  const restore = async () => {
    setBusy(true);
    try {
      const dest = await api.restoreSession(meta.session_id, target);
      alert(`已恢复到：${dest}\n在 ${meta.source === "codex" ? "Codex" : "Claude Code"} 中打开该项目即可看到会话。`);
      onDone();
    } catch (e) {
      alert(`恢复失败：${e}`);
      setBusy(false);
    }
  };

  return (
    <div className="dialog-backdrop">
      <div className="dialog">
        <h3>恢复会话：{meta.title}</h3>
        <p>原路径：{meta.origin_cwd}</p>
        <label>
          本机项目路径：
          <input value={target} onChange={(e) => setTarget(e.target.value)} size={50} />
        </label>
        <div>
          <button onClick={restore} disabled={!target || busy}>恢复</button>
          <button onClick={onClose} disabled={busy}>取消</button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建 SettingsPanel.tsx**

```tsx
import { useEffect, useState } from "react";
import { PathMapping, api } from "../api";

export function SettingsPanel() {
  const [deviceId, setDeviceId] = useState("");
  const [repoUrl, setRepoUrl] = useState("");
  const [mappings, setMappings] = useState<PathMapping[]>([]);
  const [status, setStatus] = useState("");

  useEffect(() => {
    api.getSettings().then((s) => {
      setDeviceId(s.device_id);
      setRepoUrl(s.repo_url ?? "");
      setMappings(s.path_mappings);
    });
  }, []);

  const save = async () => {
    setStatus("保存中…");
    try {
      await api.saveSettings(repoUrl || null, mappings.filter((m) => m.from_prefix && m.to_prefix));
      setStatus("已保存 ✓");
    } catch (e) {
      setStatus(`保存失败：${e}`);
    }
  };

  const setMapping = (i: number, key: keyof PathMapping, value: string) => {
    const next = mappings.slice();
    next[i] = { ...next[i], [key]: value };
    setMappings(next);
  };

  return (
    <div>
      <p>设备 ID：<code>{deviceId}</code></p>
      <label>
        同步仓库（GitHub 私有仓库，SSH 或 HTTPS，凭据用系统 git 的）：
        <input
          value={repoUrl}
          onChange={(e) => setRepoUrl(e.target.value)}
          placeholder="git@github.com:me/ai-sessions.git"
          size={50}
        />
      </label>
      <h4>路径映射（其他设备路径前缀 → 本机路径前缀）</h4>
      {mappings.map((m, i) => (
        <div key={i}>
          <input
            value={m.from_prefix}
            onChange={(e) => setMapping(i, "from_prefix", e.target.value)}
            placeholder="E:\fromGithub"
          />
          →
          <input
            value={m.to_prefix}
            onChange={(e) => setMapping(i, "to_prefix", e.target.value)}
            placeholder="/Users/me/code"
          />
          <button onClick={() => setMappings(mappings.filter((_, j) => j !== i))}>删</button>
        </div>
      ))}
      <button onClick={() => setMappings([...mappings, { from_prefix: "", to_prefix: "" }])}>
        + 加一条映射
      </button>
      <div>
        <button onClick={save}>保存</button> <span>{status}</span>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 编译验证**

Run: `npm run build`（项目根目录，跑 tsc + vite build）
Expected: 无类型错误，构建成功。

- [ ] **Step 4: 手动冒烟（真实数据走一遍闭环）**

1. 在 GitHub 上建一个**私有**空仓库（不要 README，保持全空）。
2. `npm run tauri dev` 启动应用。
3. 设置页：填仓库地址，保存 → 无报错。
4. 本机会话页：能看到真实的 Codex 与 Claude Code 会话列表（来源徽标、首条消息、时间正确）。
5. 勾选 1-2 个不含敏感内容的会话，上传 → 去 GitHub 网页确认 `sessions/<id>/transcript.jsonl` 和 `meta.json` 出现。
6. 仓库会话页：改标题、加星 → GitHub 上 meta.json 相应更新。
7. 恢复验证（单机模拟双设备）：挑一个原路径在 `E:\...` 的仓库会话，恢复到一个新建的空目录（如 `E:\tmp\restore-test`）→ 在该目录下运行 `claude --resume`（或打开 Codex）确认会话可见、能继续对话。
8. 验证不覆盖：对同一会话再恢复一次 → 报"target already exists"。

Expected: 全部通过。第 7 步是本计划的核心验收——**恢复出的会话必须能被官方工具识别并续聊**。如果 Claude Code 未列出会话，用真实会话文件对比检查是否有缺失的必需字段（这是格式随版本漂移的信号，修 scan/restore 后重跑 `cargo test`）。

- [ ] **Step 5: Commit**

```powershell
git add src/components/RestoreDialog.tsx src/components/SettingsPanel.tsx
git commit -m "feat: restore dialog and settings panel, complete sync loop"
```

---

## 整体验收清单

- [ ] `cargo test`（src-tauri 下）全绿：model / paths / scan_claude / scan_codex / config / gitcli / syncrepo / sync / restore 单测 + e2e_two_devices 集成测试。
- [ ] `npm run build` 无类型错误。
- [ ] Task 14 Step 4 的 8 步手动冒烟全部通过（尤其第 7 步：官方工具能续聊恢复的会话）。
- [ ] 上传后 `~/.claude` / `~/.codex` 下原始文件未被修改（`git status` 式对比或看文件 mtime）。

## 已知边界（本计划刻意不做，留给后续计划）

- 跨平台导入（Codex ↔ Claude Code 互转）——独立计划。
- transcript 压缩（zstd）、大文件分块——仓库超过 ~1GB 时再考虑。
- 会话内容预览/全文搜索——需要时再加本地索引（可重建缓存，不是数据真身）。
- 敏感信息扫描/加密——上传前人工判断；自动 redaction 后续计划。
- meta.json 字段级合并——当前整文件 LWW 已够用（单人多设备场景冲突极少）。
